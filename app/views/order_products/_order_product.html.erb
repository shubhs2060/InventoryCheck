<tr>
  <td><%= order_product.quantity %> x</td>
  <td colspan="2"><%= order_product.product.code %></td>
  <td><%= number_to_currency(order_product.subtotal) %></td>
  <td><%= link_to 'Edit', edit_order_product_path(order_product) %></td>
  <td><%= link_to 'Remove', order_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
<%= render order_product.order_product_histories %>